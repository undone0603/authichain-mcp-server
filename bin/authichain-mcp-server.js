#!/usr/bin/env node
// CLI entry for `npx authichain-mcp-server`. Starts the server (stdio by default;
// TRANSPORT_MODE=http for Streamable HTTP). All logic lives in dist/index.js.
import "../dist/index.js";
